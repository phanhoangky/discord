import ApiHelper from "@/api";
import { API_URL } from "../Constant";

// export const callGetUserForInvitation = async () => {
//   //
//   const { data } = await ApiHelper.get(`${API_URL.USER}/`);
// };
